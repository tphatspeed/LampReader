# macOS — Finder rải file này khắp nơi mỗi lần mở thư mục.
# Nó KHÔNG chỉ bẩn repo: một cái nằm trong lamp-reader/_locales/ là đủ làm
# Chrome hỏng phần đa ngôn ngữ, vì Chrome duyệt thư mục con ở đó để tìm locale.
# test/qa.test.js có phép kiểm bắt lại chuyện này trước khi đóng gói.
.DS_Store
**/.DS_Store

# Trình soạn thảo
.vscode/
.idea/
*.swp

# Bản đóng gói để tải lên Chrome Web Store
*.zip
lamp-reader.crx
lamp-reader.pem
